# 嵌入式作业 1：Python / C# 性能对比与 Python 优化

## 作业 1.1：Python 与 C# 运行时间对比

### 一、题目

用 Python 和 C# 各实现一段循环 100000 次的 `sqrt + pow` 运算，测量耗时并分析。

### 二、测试环境

- 机器：MacBook Air（Apple M4，arm64），macOS 26.2
- Python 3.9.6（CPython），计时用 `datetime.datetime.now()`
- C#：Mono 6.14.1，`mcs -optimize` 编译，计时用 `Stopwatch`
- 两边都重复 10 轮取平均，减小偶然误差

源码：`python_benchmark.py`、`CSharpBenchmark/Program.cs`

### 三、实测结果

| 语言   | 平均耗时（10 万次循环） | 相对倍率 |
| ------ | ----------------------- | -------- |
| C#     | 约 1.15 ms              | 1.0×     |
| Python | 约 9.33 ms              | 约 8.1×  |

C# 比 Python 快约 8 倍。

### 四、结果分析

差距主要来自三点：

1. **执行方式**：C# 经 JIT 编译为本地机器码，循环体几乎就是几条本地指令；Python 是字节码解释执行，每次迭代都要走解释器分派和 C API 调用。
2. **数据类型**：C# 的 `int` / `double` 是栈上值类型，零分配；Python 的 `int` / `float` 是堆上对象，每次运算都涉及装箱和引用计数。
3. **循环开销**：`for (int i = 0; ...)` 在 C# 里是 `inc + cmp + jl`，几乎免费；`for number in range(...)` 在 Python 里要反复调用迭代器的 `__next__`。

### 五、结论

数值密集型计算场景下，编译型语言（C#）相比解释型语言（Python）有明显优势。Python 适合做原型和上层逻辑，性能关键路径更适合用 C# / C / C++ 这类编译到本地代码的语言。

---

## 作业 1.2：Python 代码优化

### 一、目标

参考 [riverzhou/PythonSpeedTest](https://github.com/riverzhou/PythonSpeedTest) 的方案，对作业 1.1 中的 Python 代码进行多种方式的加速，并以表格形式给出对比结果。

为了让差距更明显，把数据规模从 10 万扩大到 100 万，运算保持为 `sqrt(i) + i**2`。

### 二、优化方案

参照开源项目，本次实现了从「纯 Python 循环」到「OpenMP 并行 C 模块」共 8 种实现：

| Name         | Information           | 优化思路                                               |
| ------------ | --------------------- | ------------------------------------------------------ |
| calc_py      | Standard Python Loop  | 基线，等价于作业 1.1 的写法（math + range）            |
| calc_np      | NumPy Vectorized      | 用 NumPy 数组+广播代替 for 循环（矩阵化）              |
| calc_np_opt  | NumPy Optimized (i\*i) | 在向量化基础上把 `np.power(v, 2)` 换成 `v*v`           |
| calc_jit     | Numba JIT             | `@njit` 装饰器，运行时 JIT 编译为本地代码              |
| calc_aot     | Numba AOT             | `numba.pycc` 预编译为扩展模块（.so）                   |
| calc_cy      | Cython Module         | 用 Cython 重写循环并编译为扩展模块                     |
| calc_c       | C Module (clang -O3)  | 自己写 C 代码编译成动态库，ctypes 调用                 |
| calc_omp     | C Module + OpenMP     | 在 C 实现上加 `#pragma omp parallel for` 多线程并行    |

源码与构建脚本均在 `1.2_optimized/` 目录下，`build.sh` 会一次性编译出所有原生扩展，`SpeedTest.py` 完成正确性校验和 5×5 计时。

### 三、测试环境（实验机）

题目提示「在 Windows 上实验」，本机为 macOS（Apple M4），把同一套思路移植到本地：原项目的 `.dll` 替换为 `.dylib`，OpenMP 通过 brew 的 libomp 提供，其它（NumPy / Numba / Cython）一致。

| 项目   | 取值                                              |
| ------ | ------------------------------------------------- |
| OS     | macOS 26.2 / Darwin 25.2.0 (arm64)                |
| CPU    | Apple M4，10 线程                                 |
| Python | 3.9.7（conda）                                    |
| 库     | NumPy 2.0.2 / Numba 0.60.0 / Cython 3.2.4         |
| 编译器 | Apple clang 17（C），libomp（OpenMP）             |

### 四、测试结果

数据规模 1,000,000，类型 `numpy.int32`，每项跑 5 轮 × 每轮 5 次（与开源项目一致）：

###### Test Result

| Name        | Information           |     1 |     2 |     3 |     4 |     5 |   Avg |  Faster |
|:------------|:---------------------:|------:|------:|------:|------:|------:|------:|--------:|
| calc_py     | Standard Python Loop  | 0.857 | 0.850 | 0.849 | 0.851 | 0.940 | 0.870 |   1.000 |
| calc_np     | NumPy Vectorized      | 0.040 | 0.034 | 0.036 | 0.034 | 0.036 | 0.036 |  24.297 |
| calc_np_opt | NumPy Optimized (i\*i) | 0.020 | 0.011 | 0.010 | 0.009 | 0.009 | 0.012 |  73.253 |
| calc_jit    | Numba JIT             | 0.004 | 0.003 | 0.004 | 0.003 | 0.003 | 0.003 | 255.008 |
| calc_aot    | Numba AOT             | 0.006 | 0.005 | 0.005 | 0.005 | 0.005 | 0.005 | 169.577 |
| calc_cy     | Cython Module         | 0.005 | 0.004 | 0.005 | 0.004 | 0.004 | 0.005 | 192.657 |
| calc_c      | C Module (clang -O3)  | 0.003 | 0.003 | 0.003 | 0.003 | 0.006 | 0.004 | 232.779 |
| calc_omp    | C Module + OpenMP     | 0.002 | 0.002 | 0.002 | 0.004 | 0.005 | 0.003 | 302.218 |

> 时间单位：秒。`Faster` 列为相对 `calc_py` 的加速比。

### 五、结果分析

可以分成四个层级看：

1. **从纯 Python 到 NumPy（24×）**
   `calc_np` 把循环交给 NumPy 在 C 层面用 SIMD/连续内存做批量运算，单次就完成了 100 万次 sqrt + 平方。开销从「100 万次解释器调度」降到「一次函数调用 + 一次内存遍历」。

2. **微优化 i\*i（73×）**
   `np.power(v, 2)` 走的是通用指数实现（log/exp），而 `v*v` 是一次乘法。同样是 NumPy 向量化，仅这一处就再快 3 倍，说明热点函数本身的实现成本不可忽视。

3. **JIT / AOT / Cython / C（170×–255×）**
   这一层都把循环编译成了本地代码，差距主要来自编译器/运行时的细节：
   - **calc_jit**：Numba JIT 在第一次调用时已经触发并由 `cache=True` 命中缓存，后续走的是 LLVM 优化过的本地代码，最接近手写 C；
   - **calc_aot**：Numba AOT 编译路径与 JIT 类似但少了一些运行时调度优化，略慢于 JIT；
   - **calc_cy**：Cython 通过类型注解直接调用 `libc.math` 的 sqrt，性能稳定；
   - **calc_c**：clang `-O3 -ffast-math`，向量化得很彻底，单线程已经很难再快；
   - 这一层之间的差距主要是测量噪声，量级一致。

4. **OpenMP 并行（302×）**
   `calc_omp` 在 C 实现的循环上加了 `#pragma omp parallel for`，把任务切到 10 个线程上同时算。M4 上能再多拿 ~1.3 倍，受限于这种「一遍读、一遍写」的工作几乎是内存带宽 bound，加再多线程也提升有限。

### 六、结论

- 把作业 1.1 的写法从「Python for 循环」改成「NumPy 向量化」是性价比最高的一步，几行改动换来 24× 提升，再把 `power(v, 2)` 换成 `v*v` 又能多 3 倍。
- 进一步追求性能时，**Numba JIT** 是最划算的方案：只要加一个 `@njit` 装饰器，就能拿到接近手写 C 的速度（255×），不需要写 C/构建系统。
- 当循环本身已经是瓶颈、并且数据规模足够大时，**OpenMP** 能再吃掉一点 CPU 多核红利（302×），但收益取决于内存带宽。
- 与作业 1.1 的 Python（9.33 ms / 10 万次）对照来看：在 100 万规模下，最快的 OpenMP 版本只用约 3 ms，已经显著快于原版 C# 实现，说明 Python 的「慢」并不是语言本身的硬伤，而是循环+对象模型的代价；只要把热点交给本地代码或矩阵运算，性能差距是可以填平甚至反超的。

### 七、复现步骤

```bash
# 1) 准备依赖
/opt/miniconda3/bin/pip install numpy numba cython
brew install libomp        # 若已安装可跳过

# 2) 编译所有原生模块
cd 1.2_optimized
bash build.sh

# 3) 跑测试，结果会写入 1.2_optimized/Result.md
/opt/miniconda3/bin/python SpeedTest.py
```

---

# 嵌入式作业 2：Replicable Deadlock

## 一、题目

在不同平台上重复运行给定的 Java 死锁程序，观察死锁是否能够稳定复现。题目给出的程序包含三个类：`A`、`B`、`Deadlock`。

- `A.methodA(B b)` 是 `synchronized` 方法，进入后先持有对象 `a` 的锁，再调用 `b.last()`，需要对象 `b` 的锁。
- `B.methodB(A a)` 是 `synchronized` 方法，进入后先持有对象 `b` 的锁，再调用 `a.last()`，需要对象 `a` 的锁。
- `Deadlock` 构造函数中启动子线程执行 `b.methodB(a)`，主线程忙等 `20000` 次后执行 `a.methodA(b)`。

源码与运行脚本在 `作业2_Deadlock/` 目录下：

| 文件 | 作用 |
| ---- | ---- |
| `A.java` | 定义 `methodA` 和 `last`，使用 `synchronized` 获取 A 对象锁 |
| `B.java` | 定义 `methodB` 和 `last`，使用 `synchronized` 获取 B 对象锁 |
| `Deadlock.java` | 创建两个共享对象和两个线程，触发相反顺序加锁 |
| `test_deadlock.py` | 跨平台测试脚本，编译并重复运行 100 次，超过 3 秒未退出视为死锁 |
| `run_deadlock.sh` | macOS / Linux 入口脚本，内部调用 `test_deadlock.py` |

## 二、死锁原理分析

这段程序存在经典的「交叉持锁」结构：

1. 主线程进入 `a.methodA(b)` 后持有 **A 锁**，随后尝试进入 `b.last()` 获取 **B 锁**。
2. 子线程进入 `b.methodB(a)` 后持有 **B 锁**，随后尝试进入 `a.last()` 获取 **A 锁**。
3. 如果两个线程刚好同时完成第一步，就会形成：

| 线程 | 已持有锁 | 正在等待 |
| ---- | -------- | -------- |
| 主线程 | A 锁 | B 锁 |
| 子线程 | B 锁 | A 锁 |

此时双方都不会主动释放已持有的锁，程序不能继续向下执行，即形成死锁。

按照操作系统课中死锁的四个必要条件来看，本程序满足：

| 条件 | 在程序中的体现 |
| ---- | -------------- |
| 互斥 | `synchronized` 保证同一对象锁同一时间只能被一个线程持有 |
| 占有并等待 | 一个线程持有 A 锁时继续等待 B 锁，另一个线程持有 B 锁时继续等待 A 锁 |
| 不可抢占 | Java 对象锁不能被其他线程强制抢走 |
| 循环等待 | 主线程等待子线程释放 B 锁，子线程等待主线程释放 A 锁 |

## 三、实验方法

原截图中的脚本只是直接运行 100 次：

```bash
for (( c=1; c<=100; c++ ))
do
    echo "$c times"
    java Deadlock
done
```

这个脚本的问题是：一旦某次真的死锁，后续测试会被卡住；同时 Windows 默认没有 GNU `timeout`，不方便直接复用 shell 脚本。为了能跨平台统计死锁次数，我改为使用 Python 的 `subprocess.run(..., timeout=3)` 控制每次 Java 进程的运行时间：

```bash
cd 作业2_Deadlock
bash run_deadlock.sh
```

等价的跨平台命令为：

```bash
python 作业2_Deadlock/test_deadlock.py --runs 100 --timeout 3 --log 作业2_Deadlock/run.log
```

判断规则：

- 程序正常打印 `Inside A.last()` 和 `Inside B.last()` 并退出：记为正常退出；
- 程序超过 3 秒仍未退出：由 Python 终止该 Java 进程，记为死锁；
- 程序异常退出：记为错误，不计入死锁。

## 四、本机实测结果

当前工作区可直接访问的平台为 macOS 笔记本电脑，因此本次先完成本机实测。

| 项目 | 取值 |
| ---- | ---- |
| 设备 | MacBook Air |
| CPU | Apple M4 |
| 系统 | macOS 26.2 / Darwin 25.2.0 / arm64 |
| Java | OpenJDK 25.0.2 |
| 设备形态 | Laptop / PC |
| 测试次数 | 100 |
| 超时阈值 | 3 秒 |
| 正常退出 | 100 |
| 死锁次数 | 0 |
| 死锁率 | 0% |

运行日志保存在 `作业2_Deadlock/run.log`。统计末尾如下：

```text
=== Summary ===
Total     : 100
Normal    : 100
Deadlock  : 0
Errors    : 0
Rate      : 0.0%
```

目录中早期日志 `2_deadlock/run.log` 也显示 200 次运行全部正常退出，结论与本次实测一致：在当前 macOS + Apple Silicon + OpenJDK 环境下，题目原程序虽然有死锁风险，但 100 次内没有复现死锁。

## 五、平台对比记录

题目要求比较 Linux / Windows、Desktop / Laptop、PC / Mobile device。除本机 macOS 笔记本外，本次使用 GitHub Actions 提供的 `ubuntu-latest`、`windows-latest`、`macos-latest` runner 进行云端补测。三组云端测试均使用同一份源码和同一个 Python 超时测试脚本，每个平台重复运行 100 次，单次超过 3 秒视为死锁。

| 对比维度 | 平台 / 设备 | Java 环境 | 测试次数 | 死锁次数 | 死锁率 | 结果 |
| -------- | ----------- | --------- | -------- | -------- | ------ | ---- |
| Laptop / PC | 本机 macOS MacBook Air，Apple M4 | OpenJDK 25.0.2 | 100 | 0 | 0.0% | 未复现 |
| Linux | GitHub Actions `ubuntu-latest` | Temurin OpenJDK 21 | 100 | 0 | 0.0% | 未复现 |
| Windows | GitHub Actions `windows-latest` | Temurin OpenJDK 21 | 100 | 2 | 2.0% | 低概率复现 |
| macOS / Laptop | GitHub Actions `macos-latest` | Temurin OpenJDK 21 | 100 | 1 | 1.0% | 低概率复现 |
| Mobile device | Android / Termux 或 Android Emulator | OpenJDK 21（建议） | 100 | 待测 | 待测 | 待测 |

云端三平台测试结果摘要：

| 平台 | 正常退出 | 死锁次数 | 错误次数 | 死锁率 |
| ---- | -------- | -------- | -------- | ------ |
| macOS | 99 | 1 | 0 | 1.0% |
| Windows | 98 | 2 | 0 | 2.0% |
| Ubuntu | 100 | 0 | 0 | 0.0% |

## 六、跨平台模拟与自动化方案

为了补齐 Linux、Windows、macOS 三类桌面操作系统测试，我新增了 GitHub Actions 配置：

```text
.github/workflows/deadlock-matrix.yml
```

该 workflow 使用矩阵任务自动运行：

| GitHub Actions runner | 对应平台 |
| --------------------- | -------- |
| `ubuntu-latest` | Linux |
| `windows-latest` | Windows |
| `macos-latest` | macOS |

每个 runner 都会安装 Temurin OpenJDK 21 和 Python 3.11，然后执行：

```bash
python 作业2_Deadlock/test_deadlock.py --runs 100 --timeout 3 --log deadlock-${平台}.log
```

运行结束后，三个平台的日志会作为 artifact 上传。本次已经完成一次矩阵运行，结果已补入第「五」节表格：macOS 死锁 1 次，Windows 死锁 2 次，Ubuntu 未复现。

移动设备无法由 GitHub Actions 直接提供标准 Java SE 环境，推荐使用 Android 真机或 Android Emulator + Termux 模拟：

```bash
pkg install openjdk-21 python
python 作业2_Deadlock/test_deadlock.py --runs 100 --timeout 3 --log run_android.log
```

需要注意：Android 上通常是移动 Linux 内核 + Termux/OpenJDK 环境；如果改成原生 Android App，则运行时变为 ART，已经不是题目截图中的标准 `java Deadlock` 环境。

## 七、为什么死锁没有稳定复现

这段程序的关键点是：它具备死锁结构，但没有保证两个线程一定同时持有第一把锁。

`while (count-- > 0);` 只是让主线程忙等一小段时间，试图给子线程先进入 `b.methodB(a)` 的机会。但是这个忙等时间非常依赖平台：

- CPU 越快，`20000` 次空循环越短；
- JIT 编译器可能优化循环；
- 线程启动、调度时间由操作系统决定；
- 移动设备、桌面机、笔记本的功耗策略和调度策略不同；
- 不同 JVM 对线程启动、锁实现、即时编译的细节也不同。

因此，这段程序只能说明「存在可能死锁的代码结构」，不能保证每次运行都死锁。是否在 100 次内复现，取决于主线程和子线程在极短时间窗口内的调度顺序。

## 八、结论

1. 给定程序满足死锁的四个必要条件：互斥、占有并等待、不可抢占、循环等待，因此从逻辑上存在死锁风险。
2. 本机 macOS / Apple M4 / OpenJDK 25.0.2 环境下重复运行 100 次，死锁次数为 0；GitHub Actions 中 Windows 100 次出现 2 次死锁，macOS 100 次出现 1 次死锁，Ubuntu 100 次未复现。
3. 多平台结果说明：同一份代码在不同平台上确实可能出现不同复现率，程序是否死锁高度依赖平台调度、CPU 速度、JVM 实现和忙等时间，因此“replicable deadlock”并不是由这段原始程序严格保证的。
4. 若要稳定演示死锁，应在两个线程分别拿到第一把锁后加入明确同步或延迟，例如 `Thread.sleep()`、`CountDownLatch` 等，让两个线程都先持有一把锁，再尝试请求对方的锁。

## 九、复现步骤

```bash
cd 作业2_Deadlock
bash run_deadlock.sh
```

Windows / Linux / macOS 通用命令：

```bash
python 作业2_Deadlock/test_deadlock.py --runs 100 --timeout 3 --log run.log
```

云端自动化测试：

```bash
# 将当前目录提交到 GitHub 仓库后，在 Actions 页面手动运行
Deadlock Matrix Test
```

移动设备测试可使用 Android + Termux。补测后只需把第「五」节表格中的待测项替换为对应平台的真实统计结果。
